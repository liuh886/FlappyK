{
  "name": "flappyk-browser-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "pretest:e2e": "node --check pwa.js && node --check sw.js && node --check tests/e2e/pwa.spec.js && node tests/pwa.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}

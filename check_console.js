const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log(msg.text())); page.on('pageerror', err => console.log(err.toString())); await page.goto('file://' + __dirname + '/index.html'); await page.click('#start-btn'); await new Promise(r => setTimeout(r, 1000)); await browser.close(); })();